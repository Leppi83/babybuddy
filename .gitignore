# Coverage testing files.
.coverage
/htmlcov

# Environment configuration file.
.env
.env.prod
.env.local
.env.*.local

# Virtual environment files (pipenv may use this convention).
.venv

# IDE configuration files.
/.idea

# Node development dependencies.
/node_modules
/frontend/node_modules

# Python files.
*.pyc

# App static file directories (build outputs only; root/ is source and is tracked).
/babybuddy/static/babybuddy/ant
/babybuddy/static/babybuddy/css
/babybuddy/static/babybuddy/font
/babybuddy/static/babybuddy/img
/babybuddy/static/babybuddy/js
/babybuddy/static/babybuddy/logo
/core/static
/dashboard/static
/reports/static

# Django media root directory.
/media

# Pipfile.lock.
# Committing this file makes it difficult to support multiple Python versions as
# requirements can different between versions, but the Pipfile only supports
# locking to one version.
Pipfile.lock

# Documentation
/site

# macOS files
.DS_Store

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

.vscode
.superpowers/
